"""Business contracts and deterministic domain rules.

Domain modules must remain independent from FastAPI, SQLAlchemy and external
model providers so they can be tested as pure Python functions.
"""

