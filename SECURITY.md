# 安全策略

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `0.2.x` | 支持 |
| `0.1.x` | 支持 |
| `< 0.1.0` | 不支持 |

当前项目处于 MVP 阶段，不应把匿名 API 直接暴露到公网。部署前请阅读
[生产边界和路线图](docs/roadmap.md)。

## 私下报告漏洞

不要通过公开 Issue、Discussion、Pull Request 或社交媒体披露尚未修复的漏洞。

请使用仓库的 GitHub Security Advisory 私下报告：

<https://github.com/ningkaikok/dotty-tutor/security/advisories/new>

报告请包含：

- 受影响版本或提交 SHA。
- 漏洞类型和受影响组件。
- 可重复的最小复现步骤。
- 实际影响、攻击前提和建议修复方向。
- 必要的日志或截图，并移除密钥、教材内容和个人信息。

维护者会尽量在 7 天内确认收到报告，并在调查期间提供进展。修复完成前，请为项目和用户
保留合理披露时间。

## 优先关注范围

- 身份验证或权限绕过。
- 文件上传、路径穿越、任意文件读取或写入。
- Prompt 注入导致的密钥、教材或学生数据泄漏。
- 数据库注入、跨租户访问和不安全反序列化。
- SSRF、任意命令执行和模型服务暴露。
- Azure、GitHub、数据库或模型凭据泄漏。
- 依赖供应链、Docker 镜像和 GitHub Actions 风险。

普通功能 Bug、OCR 准确率和模型输出质量问题请使用公开 Issue 模板。

## 密钥意外提交

如果密钥已经提交到 Git 历史，不要只删除文件。应立即吊销并轮换密钥，检查访问日志，
然后再清理仓库历史和部署环境。
