/**
 * Compatibility barrel for existing imports.
 *
 * Definitions live in ``types/`` by product domain. Keeping this file means a
 * learner can start with one public surface while advanced code can import a
 * focused domain module directly.
 */
export * from "./types/index";
