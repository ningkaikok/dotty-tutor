"""Contracts and offline statistics for the AI engineering benchmark line.

The package has no provider or production-runtime dependency.  Human gold data
is intentionally supplied by a separately reviewed JSONL asset; the checked-in
fixture is only for validator tests and is never counted as gold coverage.
"""
